n = int(input())
cnt = 0
num = 1

while True:
    if "666" in str(num):
        cnt += 1
    if n == cnt:
        print(num)
        break
    num += 1
___________________________________________________________________
#1
"666"이 들어가는 몇번 째 큰 수를 찾을건지 입력해라
#2
"666"이 들어간 제목의 수를 카운팅하기 위해 0으로 초기화 한다.
#3
1부터 10,000 까지의 숫자 중에 "666" 들어간 숫자를 찾기 위해 1로 초기화 한다. 
#5
num값에 "666"이 들어가 있으면 cnt값에 1을 증가하고
"666"숫자가 나온 횟수와 내가 입력한 값이 같으면 num값을 출력하고 for문을 종료한다.
#11
num값을 하나씩 증가시키면서 "666" 숫자가 나온 횟수를 cnt값에 저장한다.

  
