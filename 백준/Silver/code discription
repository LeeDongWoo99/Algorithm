n = int(input())

li = []

for i in range(n):
	m = int(input())
	if m == 0:
		li.pop()
	else :
		li.append(m)

print(sum(li))

-------------------------------------------------------------------------------------
#1
돈을 몇 번 부를지 변수 n에 입력받는다.
#3
빈 리스트 li를 선언한다.
#5
0부터 n-1까지 for문을 돌린다.
#6
for문이 반복할 때 마다 숫자(m)를 입력받는다.
#7~#10
입력받은 수가 정수"0" 이라면 리스트 li의 최근 들어온 수를 빼준다.
그 외 입력받은 숫자 m을 list li에 넣어준다.
#12
li 리스트에 있는 총 숫자를 sum함수를 이용해 총 합을 구하고 print를 통해 출력한다.


