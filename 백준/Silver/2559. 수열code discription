import sys
input = sys.stdin.readline

ls = []

n, k = map(int,input().split())
n1 = list(map(int,input().split()))

for i in range(n - k + 1):
    ls.append(sum(n1[i:i+k]))
        
print(max(ls))

-------------------------------------------------------------
#1~#2
한 두줄 입력받는 문제들과 다르게, 반복문으로 여러줄을 입력 받아야 한 때는 input()으로 입력 받는다면 시간초과가 발생할 수 있습니다.
하지만 sys.stdin.readline()을 사용하면 input보다 더 빨리 속도 차이를 보여주기 때문에 사용한다.

#4
k 값 만큼 연속적인 날짜 들의 합을 넣기 위해 ls라는 빈 리스트를 만든다. 


#6~#7
n은 온도를 측정한 전체 날짜의 수이다.
k는 합을 구하기 위한 연속적인 날짜의 수 이다.
n1은 n일 만큼 온도를 입력받는다.

#9
for문에 범위를 n-k+1로 해야 n개를 k개로 연속적인 조합으로 만들 수 있다.

#10
n1[i]부터 n1[i+k]만큼에 합을 ls 리스트에 넣는다.
for문을 돌면서 i값이 증가하면 증가한 숫자들의 k날짜 만큼의 합을 넣어준다.

#12
합들이 들어있는 ls 리스트에서 제일 큰 값을 출력한다. 

이 코드로 실행하면 정상적으로 값들이 나오지만 백준에 제출하면 시과초과가 뜨게 된다.
그래서 오류에 대해 찾아보니 "한 두줄 입력받는 문제들과 다르게, 반복문으로 여러줄을 입력 받아야 한 때는 input()으로 입력 받는다면 시간초과가 발생할 수 있습
니다.하지만 sys.stdin.readline()을 사용하면 input보다 더 빨리 속도 차이를 보여주기 때문에 사용한다." 라고 해서 다시 코드를 작성했습니다.


import sys
input = sys.stdin.readline

n, k = map(int,input().split())
n1 = list(map(int, input().split()))

ls = []
ls.append(sum(a[:k]))

for i in range(n - k):
    result.append(result[i] - n1[i] + n1[k+i])
        
print(max(result))
------------------------------------------


#1~#2
sys.stdin.readline 여러번 사용하기 편하게 input변수에 넣는다.

지나치게 많은 sum 연산을 줄이기 위해 먼저 기존에 저장한 결과 값에서 이전 값을 제외하고 이후값을 더해 값들을 구하는 방식을 사용하였습니다. 범위 안의 값을 모두 사용하지 않고 단 3개의 값만 계산하면 되므로 기존보다 실행 속도를 대폭 줄일 수 있습니다.

ex/
n = 10, k = 5
n1 = 3 -2 -4 -9 0 3 7 13 8 -3


ls[-12,-12,-3, 14, 31, 28]

-12 -3 +3 = -12
-12 +2 +7 = -3
-3 +4 +13 = 14
14 +9 +8  = 31
31 -0 +-3 = 28

전 값에서 더했던 첫번쨰 값을 빼고 추가로 넣을 값 넣으면 된다.
1~5 값을 먼저 구해서 리스트에 넣고 그다음 2~6값을 구해야 하는데 전에 구했던 값에서 1번째 값을 빼고 6번째 값을 넣으면 for문에서 sum함수를 사용안해도 구할 수 있다.





https://531522szerodesire.tistory.com/87



 












