a, b, c, d, e, f = map(int,input().split())
for x in range(-999,1000):
    for y in range(-999,1000):
        if (a * x) + (b * y) == c and (d * x) + (e * y) == f:
            print(x, y)

--------------------------------------------------------------------------------------------
  #1
  정수 6개를 입력 받는다.
  #2~#5
  ax + by = c
  dx + ey = f
  이중 for문을 통해 x,y값을 증가시키고 연립 방정식을 계산했을 떄 c, f 값과 같으면 해당하는 x, y 값을 출력한다.
