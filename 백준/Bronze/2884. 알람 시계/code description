H, M = map(int, input().split())
M -= 45
if M < 0:
        M = 60 - abs(M)
        H -= 1
        if H < 0:
            H = 24 - abs(H)

print(f"{H} {M}")
---------------------------------------------------설명___________________________________________________________________________________
#1
알람을 설정할 시(H)와 분(M)을 입력받는다.
#2
조건이 알람을 45분 일찍 설정하기 이므로 입력 받은 분(M)에 45분을 뺀다.
#3~5
시계의 분은 0~60 이기 때문에 45분을 뺐을 때 M이 0보다 작을 경우 60에서 M값을 빼고 H(시간)에서 1을 뺀다.
#6~7
시간도 0~23 이기 때문에 입력받은 분(M)에서 45분을 뺐을 때 M이 음수여서 시간(H)-1을 했을 때 음수일 경우 24에서 H값을 빼준다.


----------------------------------------------------알게된 점----------------------------------------------------------------------------------
asb()
파이썬의 abs()함수는 파이썬 표준 라이브러리에서 사용할 수 있는 내장함수 입니다. 주어진 숫자의 절대값을 반환 합니다.
ㄴ이것을 사용하지 않으면 #4, #7에서 음수값을 빼면 양수값이 되면서 올바른 값이 나오지 않는다.
